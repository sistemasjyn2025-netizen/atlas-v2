import { Unit } from '../Unit';
import { UnitFamily, UnitSystem } from '../types';

export const DensityUnits = {
  KilogramPerCubicMeter: new Unit('kg/m³', 'Kilogram per Cubic Meter', UnitFamily.Density, UnitSystem.SI),
};
