// Prisma v7 configuration
// Connection URL should be provided via DATABASE_URL environment variable at runtime.
// For local development: export DATABASE_URL="postgresql://user:pass@localhost:5432/atlas_db"
// This file is intentionally minimal — the schema is in packages/persistence/prisma/schema.prisma

module.exports = {};
